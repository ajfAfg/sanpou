mod factorial {
  var x = 0;

  def testing = finally(x == 120);

  fn fact(x) {
    if (x == 0) {
      return 1;
    } else {
      var ans = x * fact(x-1);
      return ans;
    }
  }

  fn worker() {
    x = fact(5);
    return ();
  }

  fair process workers = worker in 1..1;
}
